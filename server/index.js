import 'dotenv/config';import express from 'express';import cors from 'cors';import helmet from 'helmet';import morgan from 'morgan';import mongoose from 'mongoose';import routes from './routes/index.js';
const app=express();app.use(helmet({crossOriginResourcePolicy:false}));app.use(cors());app.use(express.json());app.use(morgan('tiny'));app.get('/health',(_,res)=>res.json({ok:true,service:'supercars-showcase-api'}));app.use('/api',routes);const port=process.env.PORT||8080;if(process.env.MONGODB_URI){mongoose.connect(process.env.MONGODB_URI).then(()=>console.log('MongoDB connected')).catch(e=>console.error(e.message))}app.listen(port,()=>console.log(`SuperCars Showcase API on ${port}`));
