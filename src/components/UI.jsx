import {motion} from 'framer-motion';import {Link} from 'react-router-dom';
export const fade={initial:{opacity:0,y:28},whileInView:{opacity:1,y:0},viewport:{once:true},transition:{duration:.7}};
export function Section({eyebrow,title,children}){return <section className="mx-auto max-w-7xl px-5 py-20"><motion.div {...fade}><p className="mb-3 text-sm uppercase tracking-[.35em] text-gold">{eyebrow}</p><h2 className="font-display text-4xl md:text-6xl">{title}</h2></motion.div>{children}</section>}
export function Glass({children,className=''}){return <motion.div {...fade} className={`rounded-[2rem] border border-white/10 bg-white/[.055] p-6 shadow-2xl backdrop-blur-xl ${className}`}>{children}</motion.div>}
export function CarCard({car}){return <Glass className="group overflow-hidden p-0"><div className="overflow-hidden"><img loading="lazy" src={car.image} className="h-64 w-full object-cover transition duration-700 group-hover:scale-110"/></div><div className="p-6"><p className="text-gold">{car.brand}</p><h3 className="font-display text-3xl">{car.name}</h3><div className="my-5 grid grid-cols-3 gap-3 text-sm text-white/70"><span>${car.price.toLocaleString()}</span><span>{car.hp} hp</span><span>{car.topSpeed} km/h</span></div><Link to={`/cars/${car.id}`} className="inline-flex rounded-full border border-gold/50 px-5 py-3 text-sm uppercase tracking-widest text-gold hover:bg-gold hover:text-black">View Details</Link></div></Glass>}
